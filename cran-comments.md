## R CMD check results

0 errors | 0 warnings | 2 notes

* The Title field is flagged for title case ("and" vs. "And") — "and" is
  intentionally lowercase as a coordinating conjunction; this is a style
  false positive.
* "unable to verify current time" — a local-clock check artifact, not
  reproducible on CRAN's build infrastructure.

## Downstream dependencies
There are currently no downstream dependencies for this package.

